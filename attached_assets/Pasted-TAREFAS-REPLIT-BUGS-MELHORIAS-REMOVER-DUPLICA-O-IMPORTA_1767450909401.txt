TAREFAS (REPLIT) — BUGS + MELHORIAS (REMOVER DUPLICAÇÃO)

IMPORTANTE:
Ignore qualquer parte sobre “Mike abriu duas conversas / duplicação”. Isso NÃO é bug (eu usei dois números com a mesma foto). 
NÃO mexer em lógica de deduplicação agora.

------------------------------------------------------------
1) BUG: Barra de rolagem da conversa não funciona (subir/descer)
------------------------------------------------------------
Tarefas:
- Localize o componente da tela de conversa (chat panel) e descubra por que o scroll não está rolando.
- Verifique: overflow CSS, height fixa, container com flex, event listeners, “scroll lock”, overlay, body overflow hidden.
- Garanta que:
  - O container do histórico tenha `overflow-y: auto` e altura calculada corretamente (flex: 1).
  - No mobile/desktop funcione mouse wheel, trackpad e arraste.
  - Não exista um elemento por cima capturando scroll.
- Adicionar “Auto-scroll inteligente”:
  - Quando chegar mensagem nova, só rolar pro final se o usuário já estiver próximo do final.
  - Se ele estiver lendo mensagens antigas, NÃO puxar pro final.
- Implementar botão “Ir para o fim” quando não estiver no final.

------------------------------------------------------------
2) BUG: “kbelludo atacadista” chamou e não criou contato automático
------------------------------------------------------------
Tarefas:
- Localizar o fluxo que cria contato ao receber primeira mensagem.
- Garantir que TODO inbound:
  - normalize telefone / chatId
  - procure contato/conversa
  - se não existir, crie automaticamente com nome fallback:
    - nome = pushName do WhatsApp se existir, senão “Contato +55...”
- Validar por logs:
Seja bem explícito nos logs em 2 pontos:
  1) ponto de entrada do evento/webhook (quando chega mensagem)
  2) ponto de criação do contato e da conversa

Logs devem conter:
- phone
- normalized_phone
- chat_id
- created_contact_id
- created_conversation_id

------------------------------------------------------------
3) MELHORIA: Macros com ordem customizável (mover sequência)
------------------------------------------------------------
Tarefas:
- No banco, adicionar campo `position` ou `sort_order` na tabela de macros.
- Implementar UI com drag-and-drop para reordenar macros.
- Salvar nova ordem no backend (endpoint /macros/reorder).
- Garantir que a lista sempre carregue ordenada por `sort_order`.

------------------------------------------------------------
4) MELHORIA: Kanban -> ao trocar coluna, trocar etiqueta automaticamente
------------------------------------------------------------
Tarefas:
- Definir mapeamento: cada coluna do Kanban tem uma etiqueta vinculada (tag/label).
- Ao mover card:
  - atualizar `stage_id`/`column_id`
  - atualizar `tag` automaticamente:
    - remover tag anterior de funil
    - aplicar a nova tag
- Backend deve garantir:
  - atualização atômica (transaction)
  - emitir evento realtime (socket) para todos operadores verem.

------------------------------------------------------------
5) MELHORIA: Campo de anotação do lado do nome no painel de conversa
------------------------------------------------------------
Tarefas:
- Adicionar campo “Nota” visível e editável ao lado do nome do contato na conversa.
- Exemplo: “ESPERANDO MERCADORIA”
- Deve ser visto por TODOS operadores.
Requisitos:
- Salvar no banco (contact.note ou conversation.note).
- Auto-save (debounce 400–700ms) e botão “Salvar” opcional.
- Mostrar “última atualização por (operador) e data/hora” (se existir user).
- Sincronizar em tempo real para todos (Socket.io).

------------------------------------------------------------
6) TESTES / CHECKLIST de validação (obrigatório)
------------------------------------------------------------
- Caso 1: receber mensagem de um número novo → cria contato e conversa automaticamente.
- Caso 2: scroll sobe/desce e auto-scroll respeita leitura antiga.
- Caso 3: mover card no Kanban → etiqueta muda e todos veem.
- Caso 4: reordenar macros → persiste após refresh.
- Caso 5: nota do contato aparece para todos operadores imediatamente.

ENTREGA:
- Informar quais arquivos foram alterados
- Como testar cada item acima
- Se algum item não puder ser feito por limitação do código atual, explicar exatamente onde e por quê
