REPLIT PROMPT — FIX @lid (Linked Device ID) TO SHOW REAL PHONE NUMBER WHEN AVAILABLE (BAILEYS)

PROBLEM:
Some contacts appear as "LID_XXXXXXXX" or "XXXXXXXX@lid" instead of the real WhatsApp phone number.
I need the CRM to display the REAL phone number whenever WhatsApp/Baileys provides it.

IMPORTANT TRUTH:
Sometimes WhatsApp does NOT provide the phone number to the web client (privacy limitation).
So we must implement the best possible resolution:
1) Use ALT JIDs provided by Baileys (remoteJidAlt / participantAlt)
2) Maintain a mapping table (lid → phone jid) when we ever receive the alternate id
3) Fallback safely if not resolvable (do NOT break chat handling)

================================================
STEP 1 — UPDATE BAILEYS + ENABLE ALT FIELDS
================================================
- Ensure we are using a Baileys version that includes ALT JID fields (v7+ or latest stable/rc).
- Read the Baileys migration/docs about:
  - remoteJidAlt (DMs)
  - participantAlt (groups/broadcast contexts)

================================================
STEP 2 — IMPLEMENT JID RESOLVER (MANDATORY)
================================================

Create ONE function used everywhere:

resolveChatIdentifiers(msg) => {
  chatIdRaw,        // msg.key.remoteJid (may be @lid or @s.whatsapp.net)
  chatIdResolved,   // phone jid if available, else @lid
  phoneDigits,      // extracted digits if resolved is phone jid
  isLid,            // boolean
  altJidUsed        // boolean
}

RULES:
1) Always read:
   chatIdRaw = msg.key.remoteJid

2) If chatIdRaw endsWith "@lid" OR contains "LID_":
   Try to resolve using ALT fields:
   - For DMs: use msg.key.remoteJidAlt (if present)
   - For other contexts: use msg.key.participantAlt (if present)
   If an alt jid exists and endsWith "@s.whatsapp.net":
     chatIdResolved = altJid
     altJidUsed = true
   else:
     chatIdResolved = chatIdRaw
     altJidUsed = false

3) If chatIdRaw is already "@s.whatsapp.net":
   chatIdResolved = chatIdRaw

4) phoneDigits:
   - If chatIdResolved endsWith "@s.whatsapp.net":
       extract digits before "@"
   - else empty

================================================
STEP 3 — STORE MAPPING lid → phone (MANDATORY)
================================================

Create a DB table/collection:
LidMapping {
  lidJid: "221800818593797@lid",
  phoneJid: "5511999999999@s.whatsapp.net",
  updatedAt
}

Whenever a message arrives where:
- chatIdRaw is @lid
- and chatIdResolved is @s.whatsapp.net

Then:
- Upsert LidMapping(lidJid -> phoneJid)

================================================
STEP 4 — ALWAYS USE RESOLVED JID FOR CRM ID (VERY IMPORTANT)
================================================

Conversations and messages MUST use chatIdResolved as the main chatId when it is a phone jid.

If it’s not resolvable:
- Use the @lid as chatId (but keep mapping table for future upgrades)
- Do NOT drop/ignore the message.
- The UI can show "Unknown number (LID)" but must still receive and display messages.

================================================
STEP 5 — UI DISPLAY PRIORITY (PHONE FIRST)
================================================

When rendering a conversation:
- If phoneJid exists: show formatted phone number (+55...)
- Else show @lid code (as fallback label)

If later we get a mapping update:
- Migrate conversation chatId from @lid to @s.whatsapp.net
- Merge message history
- Keep one single conversation

================================================
STEP 6 — MIGRATION / MERGE (OPTIONAL BUT RECOMMENDED)
================================================

If the DB already has conversations saved under @lid:
- When mapping is discovered later:
  - Move messages to the phoneJid conversation
  - Update references
  - Delete/Archive the old @lid conversation

================================================
STEP 7 — ACCEPTANCE TESTS
================================================

A) If WhatsApp provides remoteJidAlt/participantAlt:
- CRM must display the REAL phone number
- Store mapping in LidMapping

B) If WhatsApp does NOT provide phone:
- CRM must still show the chat and messages (under @lid)
- No crashes, no message loss

C) If mapping becomes available later:
- CRM must merge and update to real number

================================================
DELIVERABLES
================================================
- Show me which Baileys version is in use
- Implement resolveChatIdentifiers()
- Add LidMapping persistence
- Update message/conversation saving to use chatIdResolved
- Provide merge strategy for existing @lid chats
